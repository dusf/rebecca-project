# 自定义列弹层统一实施清单

**目标：** 统一用户管理与产品列表的自定义列弹层结构、字号、尺寸和交互，并移除可见的上下箭头。

## 任务 1：先更新契约测试

- 修改 `admin/user/tests/users_contract.test.js`
- 断言两个弹层均使用统一标题、右上角“恢复默认”和 12px 提示文案
- 断言用户管理不再渲染 `data-column-move` 上下箭头
- 断言用户管理弹层宽度为 240px
- 运行测试并确认旧实现不能通过新增断言

## 任务 2：统一共享弹层样式与产品列表结构

- 修改 `admin/common/css/commons.css`
- 为标题、重置按钮、提示文案和拖拽手柄补齐统一样式
- 修改 `admin/product/product_list.html`
- 将“恢复默认”移到标题右侧，提示文案置于列表上方，移除底部操作区
- 修改 `admin/common/js/commons.js`
- 保留拖拽排序，并为拖拽手柄补充键盘上下排序能力

## 任务 3：改造用户管理弹层

- 修改 `admin/user/js/users.js`
- 使用共享弹层 DOM 与样式类，移除可见上下箭头
- 保留显示列、恢复默认、鼠标拖拽、键盘排序和持久化逻辑
- 修改 `admin/user/css/users.css`
- 复用 240px 产品弹层规格，清理旧的 300px/箭头样式
- 更新相关静态资源版本号

## 任务 4：验证与交付

- 运行用户管理契约测试及项目相关静态检查
- 检查两个页面可访问及关键 DOM/CSS
- 提交并推送当前分支
- 写入 Rebecca Project 每日记忆，并记录“明确需求直接实施”的长期协作偏好
